const chalk = require('chalk');

console.log(chalk.bold.magenta('（ -.-）ノ-=≡≡卍\n'));
console.log(
  chalk.cyan('Visit ') +
    chalk.white.underline.bold(
      'http://localhost:5732/spotim-ninja-tools.user.js'
    ) +
    chalk.cyan(' to install the script\n')
);
