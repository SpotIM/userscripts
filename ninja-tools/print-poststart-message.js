const chalk = require('chalk');

console.log(chalk.bold.magenta('（ -.-）ノ-=≡≡卍\n'));
console.log(
  chalk.cyan('Now visit ') +
    chalk.white.underline.bold(
      'https://github.com/SpotIM/userscripts/raw/master/spotim-ninja-tools.user.js'
    ) +
    chalk.cyan(' to re-install the script from Github\n')
);
